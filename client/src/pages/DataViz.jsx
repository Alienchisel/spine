// Scratch surface for data-visualization experiments. Built to host
// Tufte-style sketches (small multiples, sparklines, range frames, etc.)
// against Spine's library data. Currently a placeholder shell; experiments
// will be slotted in over time.
export default function DataViz() {
  return (
    <div className="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-8 space-y-8">
      <h1 className="font-slab text-2xl text-parchment tracking-wide uppercase">Data visualization</h1>
      <p className="text-sm text-neutral-500">
        Scratch surface for visualization experiments. Nothing here yet.
      </p>
    </div>
  );
}
